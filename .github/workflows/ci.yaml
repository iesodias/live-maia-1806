name: MDC Workflow

on:
  push:
    branches:
      - main

jobs:

  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do Código
        uses: actions/checkout@v4

      - name: Build do codigo
        run: echo "Executando build do codigo"

  dev: 
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Fase de Desenvolvimento
        run: echo "Executando deploy em DEV"


  qa: 
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Testes de QA
        run: echo "Ëxecutando testes de automação de QA"

  homologacao:
    needs:
      - dev
      - qa

    runs-on: ubuntu-latest

    steps:
      - name: Fase de Homologacao
        run: echo "Deploy em HML"


  producao:
    needs: homologacao
    runs-on: ubuntu-latest

    steps:
      - name: Deploy em Producao
        run: echo "Ëxecutando Deploy em PROD
